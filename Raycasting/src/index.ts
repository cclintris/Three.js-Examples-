window.addEventListener("load", () => {
  const header = document.createElement("h1");
  header.innerText = "Webpack❤️T";

  const body = document.querySelector("body");
  body.appendChild(header);
});
